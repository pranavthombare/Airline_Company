use airplane_company;
db.flightBooking.insert({
});

db.flightBooking.insert({
});

db.flightBooking.insert({
});

db.flightBooking.insert({
});

db.flightBooking.insert({
});

db.flightBooking.insert({
});
